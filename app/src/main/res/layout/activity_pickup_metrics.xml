<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:orientation="vertical"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="16dp"
    tools:context=".activities.PickupMetricsActivity">

    <!-- RecyclerView for Pickup Modes -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/mode_recycler_view"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginBottom="16dp"/>

    <!-- RecyclerView for Metrics -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/metrics_recycler_view"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"/>
    <Button
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Confirm &amp; Schedule Delivery"
        android:id="@+id/schedule_delivery_btn"
        android:layout_marginTop="20dp"/>
</LinearLayout>
